# Employee Directory

Phase 1 reads employees from the canonical `Schedule` through
`EmployeeDirectoryService`. The presentation layer never traverses assignments
to assemble its own employee list. A dedicated production repository and
employee editing workflow are intentionally scheduled for SCE Phase 2.
